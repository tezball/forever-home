package com.example.foreverhome.moderation.domain;

/**
 * Type of content being moderated.
 */
public enum ContentType {
    /** Text content (name, description, health notes) */
    TEXT,

    /** Image content (pet photos) */
    IMAGE
}
