# API URL (relative path for same-origin requests from Spring Boot)
VITE_API_URL=/api

# Enable test login dropdown (set to "false" to disable)
VITE_TEST_MODE=true

# Grafana URL for admin dashboard (leave empty to hide link)
VITE_GRAFANA_URL=http://localhost:3000/d/forever-home-admin-analytics
